const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)) // parseInt(). Si antecede a la variable y la pone entre paréntesis es una función.
console.log(numero1.toString()); // .toString() es un método. Si le sigue a la variable después de un punto es un método.